BatchMath v8.35
================
Built from v8.34.

Changes
-------
1. Logarithm Rules: natural-log questions now use ln explicitly. The general log-rule base generator uses an internal ln type instead of representing natural log as log base e, preventing log_e-style display.
2. Homepage: added YouTube and Instagram logo marks directly to their social buttons while preserving the platform names, handles, and outbound links.
3. Added accessible aria-labels to both social links; decorative SVG logo artwork is hidden from screen readers.

QA
--
- JavaScript syntax checked.
- Verified no log<sub>e</sub>, log_e, or visible "log base e" patterns occur in site HTML/JS.
- Verified homepage contains one YouTube and one Instagram logo SVG and the existing social destinations are unchanged.
