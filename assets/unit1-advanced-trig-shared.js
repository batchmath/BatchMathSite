(function(){'use strict';
const R=()=>window.BatchMathRNG.random(),ri=(a,b)=>Math.floor(R()*(b-a+1))+a,pick=a=>a[ri(0,a.length-1)];
const gcd=(a,b)=>b?gcd(b,a%b):Math.abs(a)||1,rat=(n,d=1)=>{if(d<0){n=-n;d=-d;}const g=gcd(n,d);return{kind:'rat',n:n/g,d:d/g};},surd=(n,d=1,rad=2)=>({kind:'surd',n,d,rad});
const defs=[
 {id:'tan-minus-sin-square',level:'challenging',group:'identity',label:'Tan-minus-sin over a square',make(){const a=ri(1,5),m=ri(1,4),d=pick([1,2,3,4]),ans=rat(0);return{id:`tmss-${a}-${m}-${d}`,q:`\\(\\displaystyle \\lim_{x\\to0}\\frac{${m===1?'':m}(\\tan(${a}x)-\\sin(${a}x))}{${d===1?'':d}x^2}\\)`,ans,hint:'Rewrite tangent as sine over cosine and factor the numerator.',tech:'Trig identity and standard limits',sol:`<ol class="method-steps"><li>Use \\(\\tan u-\\sin u=\\sin u(1-\\cos u)/\\cos u\\) with \\(u=${a}x\\).</li><li>Then \\(\\frac{\\tan(${a}x)-\\sin(${a}x)}{x^2}=\\frac{\\sin(${a}x)}{x}\\frac{1-\\cos(${a}x)}{x}\\frac1{\\cos(${a}x)}\\).</li><li>The factors approach \\(${a}\\), \\(0\\), and \\(1\\), so the limit is \\(0\\). This contrasts with a cubic denominator, which retains a nonzero second-order cosine factor.</li></ol>`};}},
 {id:'quadratic-cos-linear',level:'challenging',group:'identity',label:'Cosine quadratic over x',make(){return{id:'qcl-fixed',q:'\\(\\displaystyle \\lim_{x\\to0}\\frac{\\cos^2x-3\\cos x+2}{x}\\)',ans:rat(0),hint:'Factor the numerator as a polynomial in cos x.',tech:'Factoring and cosine limit',sol:'<ol class="method-steps"><li>Factor: \\(\\cos^2x-3\\cos x+2=(\\cos x-1)(\\cos x-2)\\).</li><li>Write \\(\\frac{\\cos x-1}{x}(\\cos x-2)\\). The first factor tends to \\(0\\) by the conjugate cosine limit, and the second tends to \\(-1\\).</li><li>The limit is \\(0\\).</li></ol>'};}},
 {id:'quadratic-cos-square',level:'challenging',group:'identity',label:'Cosine quadratic over x squared',make(){return{id:'qcs-fixed',q:'\\(\\displaystyle \\lim_{x\\to0}\\frac{\\cos^2x-3\\cos x+2}{x^2}\\)',ans:rat(1,2),hint:'Factor in cos x, then isolate (1-cos x)/x².',tech:'Factoring and second-order cosine limit',sol:'<ol class="method-steps"><li>Factor: \\(\\cos^2x-3\\cos x+2=(1-\\cos x)(2-\\cos x)\\).</li><li>The quotient is \\(\\frac{1-\\cos x}{x^2}(2-\\cos x)\\).</li><li>The factors approach \\(1/2\\) and \\(1\\), so the limit is \\(1/2\\). The square denominator preserves the second-order term that vanished with denominator \\(x\\).</li></ol>'};}},
 {id:'nonzero-exact-substitution',level:'challenging',group:'nonzero',label:'Exact substitution at a nonzero angle',make(){return{id:'nzes-pi6',q:'\\(\\displaystyle \\lim_{x\\to\\pi/6}\\frac{\\sin(2x)}{\\sin x}\\)',ans:surd(1,1,3),hint:'The denominator is nonzero at π/6, so direct substitution is valid.',tech:'Nonzero-angle exact substitution',sol:'<ol class="method-steps"><li>Because \\(\\sin(\\pi/6)\\ne0\\), substitute directly.</li><li>\\(\\frac{\\sin(\\pi/3)}{\\sin(\\pi/6)}=\\frac{\\sqrt3/2}{1/2}=\\sqrt3\\).</li></ol>'};}},
 {id:'linear-combo-polynomial',level:'challenging',group:'sums',label:'Linear combination with a vanishing polynomial term',make(){const a=ri(2,6),b=ri(2,6),A=ri(1,4),B=ri(1,4),C=pick([-4,-2,2,4]),ans=rat(A*a-B*b);return{id:`lcp-${a}-${b}-${A}-${B}-${C}`,q:`\\(\\displaystyle \\lim_{x\\to0}\\frac{${A}\\sin(${a}x)-${B}\\tan(${b}x)+${C}x^2}{x}\\)`,ans,hint:'Split the quotient into two standard ratios and one polynomial term.',tech:'Sum decomposition and standard ratios',sol:`<ol class="method-steps"><li>Separate: \\(${A}\\frac{\\sin(${a}x)}x-${B}\\frac{\\tan(${b}x)}x+${C}x\\).</li><li>Rewrite \\(\\sin(${a}x)/x=${a}[\\sin(${a}x)/(${a}x)]\\to${a}\\) and similarly \\(\\tan(${b}x)/x\\to${b}\\).</li><li>The polynomial term \\(${C}x\\to0\\), so the limit is \\(${A*a-B*b}\\).</li></ol>`};}}
];
function generate(group='mixed'){const pool=group==='mixed'?defs:defs.filter(d=>d.group===group),d=pick(pool.length?pool:defs),p=d.make();return{...p,family:d};}
window.BMAdvancedTrigShared={defs,generate};
})();
